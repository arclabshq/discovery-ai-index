# Candidate discovery is safe-off unless explicitly enabled.
INTAKE_ENABLED=false
INTAKE_LOOKBACK_DAYS=14
INTAKE_MAX_RESULTS=12

# Configure these as secret production values before using protected write routes.
INTAKE_TOKEN=
EDITORIAL_TOKEN=
